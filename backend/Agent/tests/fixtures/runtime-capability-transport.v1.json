{
  "version": 1,
  "limits": {
    "actions": 128,
    "publicActionId": 64,
    "schemaDepth": 12,
    "schemaProperties": 256,
    "propertyName": 128,
    "enumValues": 128,
    "analyticsValues": 32
  },
  "acceptedDescriptor": {
    "id": "fixture__transport_contract",
    "name": "fixture__transport_contract",
    "plugin": "fixture-plugin",
    "category": "application",
    "description": "Exercise every schema keyword whose execution semantics cross the host-to-Agent boundary.",
    "parameters": {
      "type": "object",
      "minProperties": 3,
      "maxProperties": 6,
      "additionalProperties": {
        "type": "integer",
        "minimum": 0
      },
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["safe", "strict"],
          "default": "safe"
        },
        "fixed": {
          "type": "string",
          "const": "transport-v1"
        },
        "tags": {
          "type": "array",
          "minItems": 1,
          "maxItems": 3,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 32
          }
        }
      },
      "required": ["mode", "fixed", "tags"]
    },
    "analytics": {
      "operations": ["statistics", "threshold"],
      "dataKinds": ["scalar-raster"],
      "requiresScalar": true
    }
  }
}
